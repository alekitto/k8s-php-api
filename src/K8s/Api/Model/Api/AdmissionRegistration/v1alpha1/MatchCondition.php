<?php

/**
 * This file was automatically generated by k8s/api-generator 0.12.0 for API version v1.30.2
 *
 * (c) Chad Sikorra <Chad.Sikorra@gmail.com>
 *
 * For the full copyright and license information, please view the LICENSE
 * file that was distributed with this source code.
 */

declare(strict_types=1);

namespace K8s\Api\Model\Api\AdmissionRegistration\v1alpha1;

use K8s\Core\Annotation as Kubernetes;

class MatchCondition
{
    /**
     * @Kubernetes\Attribute("expression",isRequired=true)
     * @var string
     */
    protected $expression;

    /**
     * @Kubernetes\Attribute("name",isRequired=true)
     * @var string
     */
    protected $name;

    /**
     * @param string $expression
     * @param string $name
     */
    public function __construct(string $expression, string $name)
    {
        $this->expression = $expression;
        $this->name = $name;
    }

    /**
     * Expression represents the expression which will be evaluated by CEL. Must evaluate to bool. CEL
     * expressions have access to the contents of the AdmissionRequest and Authorizer, organized into CEL
     * variables:
     *
     * 'object' - The object from the incoming request. The value is null for DELETE requests. 'oldObject'
     * - The existing object. The value is null for CREATE requests. 'request' - Attributes of the
     * admission request(/pkg/apis/admission/types.go#AdmissionRequest). 'authorizer' - A CEL Authorizer.
     * May be used to perform authorization checks for the principal (user or service account) of the
     * request.
     *   See https://pkg.go.dev/k8s.io/apiserver/pkg/cel/library#Authz
     * 'authorizer.requestResource' - A CEL ResourceCheck constructed from the 'authorizer' and configured
     * with the
     *   request resource.
     * Documentation on CEL: https://kubernetes.io/docs/reference/using-api/cel/
     *
     * Required.
     */
    public function getExpression(): string
    {
        return $this->expression;
    }

    /**
     * Expression represents the expression which will be evaluated by CEL. Must evaluate to bool. CEL
     * expressions have access to the contents of the AdmissionRequest and Authorizer, organized into CEL
     * variables:
     *
     * 'object' - The object from the incoming request. The value is null for DELETE requests. 'oldObject'
     * - The existing object. The value is null for CREATE requests. 'request' - Attributes of the
     * admission request(/pkg/apis/admission/types.go#AdmissionRequest). 'authorizer' - A CEL Authorizer.
     * May be used to perform authorization checks for the principal (user or service account) of the
     * request.
     *   See https://pkg.go.dev/k8s.io/apiserver/pkg/cel/library#Authz
     * 'authorizer.requestResource' - A CEL ResourceCheck constructed from the 'authorizer' and configured
     * with the
     *   request resource.
     * Documentation on CEL: https://kubernetes.io/docs/reference/using-api/cel/
     *
     * Required.
     *
     * @return static
     */
    public function setExpression(string $expression)
    {
        $this->expression = $expression;

        return $this;
    }

    /**
     * Name is an identifier for this match condition, used for strategic merging of MatchConditions, as
     * well as providing an identifier for logging purposes. A good name should be descriptive of the
     * associated expression. Name must be a qualified name consisting of alphanumeric characters, '-', '_'
     * or '.', and must start and end with an alphanumeric character (e.g. 'MyName',  or 'my.name',  or
     * '123-abc', regex used for validation is '([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9]') with an optional
     * DNS subdomain prefix and '/' (e.g. 'example.com/MyName')
     *
     * Required.
     */
    public function getName(): string
    {
        return $this->name;
    }

    /**
     * Name is an identifier for this match condition, used for strategic merging of MatchConditions, as
     * well as providing an identifier for logging purposes. A good name should be descriptive of the
     * associated expression. Name must be a qualified name consisting of alphanumeric characters, '-', '_'
     * or '.', and must start and end with an alphanumeric character (e.g. 'MyName',  or 'my.name',  or
     * '123-abc', regex used for validation is '([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9]') with an optional
     * DNS subdomain prefix and '/' (e.g. 'example.com/MyName')
     *
     * Required.
     *
     * @return static
     */
    public function setName(string $name)
    {
        $this->name = $name;

        return $this;
    }
}
